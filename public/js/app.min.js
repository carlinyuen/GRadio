var socket=io.connect(window.location.href),app=app||{};$(function(){var $inputField=$("#input"),$messages=$("#messages"),$sendButton=$("#send");socket.on("message",function(data){var copy=$messages.html();$messages.html("<p>"+copy+data.msg+"</p>"),$messages.scrollTop($messages[0].scrollHeight-$messages.height())}),$sendButton.click(function(){var message=$inputField.val();message.length&&socket.emit("message",{msg:message},function(){$inputField.val("")})}),$inputField.keydown(function(e){13==e.keyCode&&$sendButton.trigger("click")})});